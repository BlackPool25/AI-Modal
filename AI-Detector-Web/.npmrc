# Force legacy peer deps (if needed for React 18 compatibility)
legacy-peer-deps=false

# Use npm registry
registry=https://registry.npmjs.org/

# Save exact versions
save-exact=false

# Engine strict (warn instead of error)
engine-strict=false

